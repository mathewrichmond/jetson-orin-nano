# System Configuration

**DEPRECATED**: packages.yaml and python_packages.yaml have been removed.

**Docker-First Architecture**: 
- System packages → Moved to `perception/Dockerfile` and `control/Dockerfile`
- Python packages → Moved to `perception/requirements.txt` and `control/requirements.txt`

See `docs/setup/DOCKER_FIRST_SETUP.md` for migration guide.
