/**:
  ros__parameters:
    # Audio device (use 'default' for default device, or specific device name)
    device: 'default'

    # Audio parameters
    sample_rate: 16000  # Hz
    channels: 1  # Mono
    format: 'S16_LE'  # Signed 16-bit little-endian

    # Capture parameters
    chunk_size: 1024  # Bytes per chunk
    publish_rate: 10.0  # Hz
